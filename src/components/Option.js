import '../icons/selected'
import config from 'veui/managers/config'

config.defaults({
  icons: {
    checked: 'selected'
  },
  ui: {
    style: {
      values: ['checkmark']
    }
  }
}, 'option')
